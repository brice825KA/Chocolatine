name: date commit
run-name: ${{ github.actor }} is learning Github Actions
on:
    push:
      branches: [main]
jobs:
  build:
    runs-on: ubuntu-24.04:
    steps:
      -run: date
      uses: actions/checkout@v4